#!/bin/bash

source /usr/share/yunohost/helpers

ynh_die "MFA Sidecar does not support change_url. It must remain installed on its own dedicated domain at '/'. Remove and reinstall on the target domain instead."
